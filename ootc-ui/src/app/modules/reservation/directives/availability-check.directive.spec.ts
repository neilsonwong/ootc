import { AvailabilityCheckDirective } from './availability-check.directive';

describe('AvailabilityCheckDirective', () => {
  it('should create an instance', () => {
    // const directive = new AvailabilityCheckDirective();
    // expect(directive).toBeTruthy();
  });
});
